<div class="page-header">
  <h1>Editing Student</h1>
</div>

<%= render 'form' %>

<hr/>

<% if admin_logged_in? %>
  <%= link_to 'Back to Admin', list_students_path(@admin)  ,class: 'btn btn-sm btn-success'%>
<% elsif faculty_logged_in? %>
  <%= link_to 'Back to Admin', students_faculty_path(@faculty)  ,class: 'btn btn-sm btn-success'%>
<% elsif student_logged_in? %>
  <%= link_to 'Back', @student  ,class: 'btn btn-sm btn-success'%>
<% end %>